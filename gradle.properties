org.gradle.jvmargs=-Xmx700m -XX:MaxMetaspaceSize=320m -XX:+UseSerialGC -XX:TieredStopAtLevel=1 -Dfile.encoding=UTF-8
org.gradle.parallel=false
org.gradle.workers.max=1
# Run the Kotlin compiler inside the Gradle daemon (avoids a second JVM on low-memory machines)
kotlin.compiler.execution.strategy=in-process

# Minecraft / Fabric (1.21.11 is OBFUSCATED -> Loom remap mode + official Mojang mappings)
minecraft_version=1.21.11
loader_version=0.19.3
fabric_version=0.141.6+1.21.11

# Kotlin (matches Fabric Language Kotlin)
flk_version=1.13.13+kotlin.2.4.10

# Mixin extras (bundled)
mixinextras_version=0.5.4

# Project
mod_version=2.14.2+fabric.1.21.11.R4
maven_group=me.muksc
archives_base_name=tacztweaks

# Optional integration compile deps
modmenu_version=17.0.0
