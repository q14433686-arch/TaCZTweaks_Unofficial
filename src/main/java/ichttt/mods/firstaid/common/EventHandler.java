package ichttt.mods.firstaid.common;

/** Compile-time stub (First Aid has no 1.21.11 NeoForge build; the compat mixin stays
 *  dormant via ModMixinPlugin — see WP-TWEAKS-0 audit A4). */
public final class EventHandler {
    private EventHandler() {
    }
}
